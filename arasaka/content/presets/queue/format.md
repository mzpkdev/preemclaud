Return JSON only.

The top-level object must contain an `issues` array. Each issue object describes one issue publication decision and must
contain concrete implementation work backed by repository evidence. Every issue must identify the affected files, list
verifiable requirements, and declare at least one scope exclusion.
