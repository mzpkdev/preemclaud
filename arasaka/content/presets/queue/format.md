Return JSON only.

The top-level object must contain an `issues` array. Each issue object describes one issue publication decision and must
contain concrete implementation work backed by repository evidence. Every issue must provide context explaining the
motivation, identify the affected files as structured objects, list acceptance criteria with verification commands, and
declare at least one scope exclusion.
