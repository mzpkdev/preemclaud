{
  "type": "object",
  "properties": {
    "actions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["warn_stale", "close_stale", "reply_question", "add_labels"] },
          "entity": { "type": "string", "enum": ["issue", "pull_request"] },
          "number": { "type": "integer" },
          "title": { "type": "string" },
          "reason": { "type": "string" },
          "comment": { "type": "string" },
          "labels_to_add": {
            "type": "array",
            "items": { "type": "string" }
          },
          "labels_to_remove": {
            "type": "array",
            "items": { "type": "string" }
          }
        },
        "required": ["type", "entity", "number", "title", "reason"],
        "additionalProperties": false
      }
    },
    "summary": { "type": "string" }
  },
  "required": ["actions", "summary"],
  "additionalProperties": false
}
