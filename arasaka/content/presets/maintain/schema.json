{
  "type": "object",
  "properties": {
    "actions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["warn_stale", "close_stale", "reply_question", "add_labels", "report_failure"] },
          "entity": { "type": "string", "enum": ["issue", "pull_request"] },
          "number": { "type": "integer" },
          "title": { "type": "string" },
          "reason": { "type": "string" },
          "comment": { "type": "string" },
          "labels_to_add": {
            "type": "array",
            "items": { "type": "string" }
          },
          "labels_to_remove": {
            "type": "array",
            "items": { "type": "string" }
          },
          "run_id": { "type": "integer" },
          "run_url": { "type": "string" },
          "workflow_name": { "type": "string" }
        },
        "required": ["type", "entity", "title", "reason"],
        "additionalProperties": false
      }
    },
    "summary": { "type": "string" }
  },
  "required": ["actions", "summary"],
  "additionalProperties": false
}
