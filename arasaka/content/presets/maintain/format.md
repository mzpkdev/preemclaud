Return JSON only.

The top-level object must contain an `actions` array and a `summary` string. Each action object describes one
maintenance operation targeting a specific issue or pull request by number.
