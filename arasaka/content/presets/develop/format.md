Return JSON only.

The top-level object must have `status: "implemented"` with:
- `pull_request`: metadata for the PR title and body
- `issue_comment`: the issue update to publish after the branch is pushed
